name: "Test of publish"

on:
  push:
    tags:
      releases/[1-9]+.[0-9]+.[0-9]+

jobs:
  build_release:

    runs-on:
      ubuntu-latest

    steps:
      - name: "Getting the sources"
        uses: actions/checkout@v2

      - name: "Build"
        run: |
          mkdir build
          echo "$GITHUB_REF" > build/v.txt

      - name: "Publish"
        uses: actions/upload-artifact@v2
        with:
          name: congenial-broccoli-${{ github.ref }}.zip
          path: ./build
